# public 项目开发规则

## 项目边界

`public` 是宣传内容创作的总入口和编排层。它管理：

- Skill 注册表和版本引用
- 公共输入输出协议
- 任务状态和人工审核节点
- 多平台结果汇总
- 仓库级验证

具体平台的写作规则、模板、脚本和示例属于独立 Skill，不复制到本仓库。

## 开发规则

- 先读规则、README、manifest 和相关 Skill。
- 一个任务只做一个 feature，使用独立 `feature/*` worktree。
- 先检查分支和工作区，再修改文件。
- 做最小的正确改动，不提前建设插件运行时、数据库或发布接口。
- 事实、版本和配置只保留一个权威来源。
- 不把本地绝对路径、密钥、Cookie 或账号信息写入发布文件。
- 不提交、推送、合并或发布，除非用户明确要求。
- 完成后运行 `scripts/verify` 和相关 Skill 的验证命令。

## 当前架构

```text
public 总入口
  -> manifest/skills.json
  -> content brief
  -> 独立平台 Skill
  -> 人工审核
  -> 平台输出
```

当前不实现运行时插件系统。Agent 通过 Skill 名称、能力声明和输入输出协议完成调用；以后有稳定需求再增加 CLI 或 API。

## 任务完成标准

- 公共协议有 schema。
- 外部 Skill 有版本、来源和能力记录。
- 单个平台失败不影响其他平台结果。
- 事实审核和最终发布审核边界清楚。
- `scripts/verify` 通过。
- Git diff 没有临时文件、个人路径和密钥。
